Attachments = new Mongo.Collection("attachments");

Meteor.startup(function() {
});

if (Meteor.isClient) {

}

if (Meteor.isServer) {

  Meteor.startup(function () {
    // code to run on server at startup

  });

}
